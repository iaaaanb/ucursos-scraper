#!/bin/bash
set -e

# Post-installation script for ucursito

echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "Installing Ucursito - U-Cursos Scraper"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

# Create virtual environment
echo "📦 Creating Python virtual environment..."
if command -v python3 >/dev/null 2>&1; then
    python3 -m venv /opt/ucursito/venv

    # Install Python dependencies into the virtual environment
    echo "📦 Installing Python dependencies into venv..."
    if [ -f /opt/ucursito/requirements.txt ]; then
        if /opt/ucursito/venv/bin/pip install -r /opt/ucursito/requirements.txt --quiet --disable-pip-version-check; then
            echo "✓ Dependencies installed successfully"
        else
            echo "⚠️  Warning: Could not install dependencies automatically"
            echo "   Please run manually:"
            echo "   /opt/ucursito/venv/bin/pip install -r /opt/ucursito/requirements.txt"
        fi
    else
        echo "⚠️  Warning: requirements.txt not found"
    fi
else
    echo "⚠️  Warning: python3 not found"
fi

# Create symlink to make ucursito available system-wide
echo "🔗 Creating symlink /usr/local/bin/ucursito..."
ln -sf /opt/ucursito/ucursito /usr/local/bin/ucursito

# Make sure the wrapper script is executable
chmod +x /opt/ucursito/ucursito

# Make sure main.py is executable
chmod +x /opt/ucursito/src/main.py

echo ""
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "✅ Ucursito installed successfully!"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""
echo "📍 Installation location: /opt/ucursito"
echo "🔗 Command: ucursito (available from anywhere)"
echo ""
echo "🚀 Quick start:"
echo "   1. Run: ucursito"
echo "   2. Enter your U-Cursos credentials (first time only)"
echo "   3. Your files will be downloaded to ./downloads/"
echo ""
echo "📖 Usage examples:"
echo "   ucursito              # Sync all sections"
echo "   ucursito -c           # Export calendar only"
echo "   ucursito -m           # Download material docente only"
echo "   ucursito -mt          # Download material + tareas"
echo "   ucursito --serve-calendar  # Start calendar server"
echo ""
echo "📚 Documentation: /opt/ucursito/README.md"
echo "🔑 Credentials: Stored in ~/.config/ucursito/credentials"
echo ""
echo "Need help? Run: ucursito --help"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

exit 0
